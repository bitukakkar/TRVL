import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
  blackdropContainer: {
    position: "absolute",
    height: "100%",
    width: "100%",
    background: "#000",
    top: 0,
    opacity: 0.5,
  },
}));
